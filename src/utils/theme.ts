interface Theme {
  color: {
    primaryColor: string;
    secondaryColor: string;
    bg: string;
    bgBtn: string;
    accentColor: string;
  };
}

export const theme: Theme = {
  color: {
    primaryColor: "#000000",
    secondaryColor: "#373737",
    bg: "#ffffff",
    bgBtn: "#EBD8FF",
    accentColor: "#5CD3A8",
  },
};
